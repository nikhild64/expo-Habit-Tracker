/**
 * Convenience re-export for the Routines store.
 * Prefer importing directly from '@/contexts/RoutinesContext' in new code.
 */
export { useRoutinesStore as useRoutines } from '@/contexts/RoutinesContext';
